# Since uv is not used, this file is now in standard pip requirements.txt format.
# It pins the primary dependencies to their latest stable versions that are >24h old.
# Run `pip install -r pylock.toml` to install them.
# To fully lock transitive dependencies, run: pip freeze > pylock.toml

hatchling==1.29.0
packaging==26.1
typer==0.24.1
rich==15.0.0
pytest==9.0.3
